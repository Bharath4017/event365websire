import { WeekDayNamePipe } from './week-day-name.pipe';

describe('WeekDayNamePipe', () => {
  it('create an instance', () => {
    const pipe = new WeekDayNamePipe();
    expect(pipe).toBeTruthy();
  });
});
